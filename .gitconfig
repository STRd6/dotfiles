[alias]
  br = branch
  ci = commit
  co = checkout
  dc = diff --cached
  df = diff
  graph = log --graph --oneline --decorate=short --branches='*'
  lg = log -p
  sh = show
  st = status -sb

[color]
  branch = auto
  diff = auto
  status = auto
  showbranch = auto
  ui = true
